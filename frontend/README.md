# Frontend — Hướng dẫn Tuần 1

Frontend dùng Next.js (app router). Tuần 1: implement Auth pages (login/register/profile) và Layout + coins list.

Phát triển local (đề xuất):

```powershell
cd frontend
npm install
npm run dev
```

FE team:
- Kiệt: auth, profile
- Khoa: homepage, market UI

Xem cấu trúc mẫu trong `src/` (placeholder).
