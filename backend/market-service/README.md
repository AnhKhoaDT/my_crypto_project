# Market Service

Service trả về danh sách coin và giá mẫu.

Start locally:

```powershell
cd backend/market-service
npm install
npm run start
```

Endpoints:
- GET /api/market/coins
- GET /api/market/price?symbol=BTC
