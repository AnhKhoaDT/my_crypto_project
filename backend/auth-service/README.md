# Auth Service

Service xác thực (register/login/me).

Env sample: See `.env.example`.

Start locally:

```powershell
cd backend/auth-service
npm install
npm run start
```

Nếu chạy chung với Docker Compose thì service sẽ tự kết nối đến Postgres được cấu hình trong `docker-compose.yml`.
